@model DXWebApplication1.Model.SchedulerDataSource

@{
    var scheduler = Html.DevExpress().Scheduler(DXWebApplication1.Model.SchedulerHelper.Settings);
    if(ViewBag.GeneralError != null) {
        scheduler.SetErrorText(ViewBag.GeneralError);
    }
}
@scheduler.Bind(Model.Appointments, Model.Resources).GetHtml()